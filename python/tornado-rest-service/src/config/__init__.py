# from os import path

# from config.static import load_config

# # Make filepaths relative to settings.
# path = lambda root, *a: path.join(root, *a)
# ROOT = path.dirname(path.abspath(__file__))
# conf = load_config('src/config')
